backend:
  name: test-repo

media_folder: assets/uploads

collections:
  - name: 'posts'
    label: 'Posts'
    label_singular: 'Post'
    folder: '_posts'
    create: true
    fields:
      - { label: 'Title', name: 'title', widget: 'string', tagname: 'h1' }
